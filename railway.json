{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mvn clean package -DskipTests",
    "runCommand": "java -jar target/pdf-api-*.jar"
  },
  "deploy": {
    "startCommand": "java -jar target/pdf-api-*.jar",
    "healthcheckPath": "/actuator/health"
  }
}
